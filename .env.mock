# Build-time environment for the E2E suite: the app runs against the scripted
# MockBackend instead of Tauri IPC, so `vite build --mode mock` produces a bundle
# Playwright can drive from a plain browser (no Tauri shell, issue #13).
VITE_BACKEND=mock
