# Local, untracked tooling state
.claude/settings.local.json

# Build and test artifacts
dist
coverage
playwright-report
test-results

# Rust side: formatted by rustfmt, generated by Tauri
src-tauri/target
src-tauri/gen
src-tauri/Cargo.lock

# Generated / vendored — never hand-formatted
src/core/types.gen.ts
package-lock.json

# Approved visual reference — kept byte-identical to the design authority
docs/maxwell-control-mono.html

# Contract fixtures: recorded headsetcontrol output, kept byte-identical to what
# the binary printed. One of them is deliberately truncated mid-string, which is
# the point — the parser must reject it — and Prettier cannot parse it at all.
docs/fixtures

# Prose is not formatted: Prettier rewrites list markers and continuation
# indentation in the hand-written specs/ADRs, which changes how they render
# (see docs/decisions/0003-lint-and-format-toolchain.md).
*.md
